{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ \"$RAILWAY_SERVICE_NAME\" = \"worker\" ]; then $HOME/.local/bin/uv run --package rkpr-worker arq worker.main.WorkerSettings; else $HOME/.local/bin/uv run --package rkpr-api uvicorn app.main:app --host 0.0.0.0 --port $PORT --app-dir apps/api; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
